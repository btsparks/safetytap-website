---
import Base from '../layouts/Base.astro';
---

<Base title="Contact" description="Get in touch with the SafetyTAP team. Request a demo or ask us anything about building hazard recognition skills in your crew.">
  <section class="max-w-3xl mx-auto px-6 py-16 md:py-24">
    <h1 class="font-display text-3xl md:text-4xl font-bold text-navy tracking-tight">Let's Talk</h1>
    <p class="text-lg text-dark/70 mt-4 leading-relaxed">Whether you're a safety manager looking to try SafetyTAP with your crew, or you just want to learn more about how it works — reach out.</p>

    <form
      name="contact"
      method="POST"
      data-netlify="true"
      action="/contact?success=true"
      class="mt-10 space-y-6"
    >
      <input type="hidden" name="form-name" value="contact" />

      <div>
        <label for="name" class="block text-sm font-medium text-dark mb-1">Name</label>
        <input type="text" id="name" name="name" required
          class="w-full border border-gray-300 rounded-lg px-4 py-3 focus:outline-none focus:border-teal focus-visible:ring-2 focus-visible:ring-teal focus-visible:ring-offset-2 transition-colors" />
      </div>

      <div>
        <label for="email" class="block text-sm font-medium text-dark mb-1">Email</label>
        <input type="email" id="email" name="email" required
          class="w-full border border-gray-300 rounded-lg px-4 py-3 focus:outline-none focus:border-teal focus-visible:ring-2 focus-visible:ring-teal focus-visible:ring-offset-2 transition-colors" />
      </div>

      <div>
        <label for="company" class="block text-sm font-medium text-dark mb-1">Company</label>
        <input type="text" id="company" name="company"
          class="w-full border border-gray-300 rounded-lg px-4 py-3 focus:outline-none focus:border-teal focus-visible:ring-2 focus-visible:ring-teal focus-visible:ring-offset-2 transition-colors" />
      </div>

      <div>
        <label for="role" class="block text-sm font-medium text-dark mb-1">Your Role</label>
        <select id="role" name="role"
          class="w-full border border-gray-300 rounded-lg px-4 py-3 focus:outline-none focus:border-teal focus-visible:ring-2 focus-visible:ring-teal focus-visible:ring-offset-2 bg-white transition-colors">
          <option value="">Select one</option>
          <option value="safety-manager">Safety Manager / Director</option>
          <option value="superintendent">Superintendent</option>
          <option value="project-manager">Project Manager</option>
          <option value="field-worker">Field Worker</option>
          <option value="operations">Operations Leadership</option>
          <option value="other">Other</option>
        </select>
      </div>

      <div>
        <label for="message" class="block text-sm font-medium text-dark mb-1">What are you looking for?</label>
        <textarea id="message" name="message" rows="4"
          class="w-full border border-gray-300 rounded-lg px-4 py-3 focus:outline-none focus:border-teal focus-visible:ring-2 focus-visible:ring-teal focus-visible:ring-offset-2 transition-colors"
          placeholder="Tell us about your crew, your safety challenges, or just ask a question."></textarea>
      </div>

      <button type="submit"
        class="bg-teal hover:bg-teal-light text-white px-7 py-3.5 rounded-lg font-semibold
               shadow-lg shadow-teal/20 transition-all duration-200
               hover:shadow-xl hover:shadow-teal/30 hover:-translate-y-0.5
               focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-teal focus-visible:ring-offset-2">
        Send Message
      </button>
    </form>

    {Astro.url.searchParams.get('success') && (
      <div class="mt-8 bg-teal-pale border border-teal/20 rounded-lg p-4">
        <p class="text-teal font-medium">Message sent. We'll get back to you shortly.</p>
      </div>
    )}
  </section>
</Base>
